function soma (n1=0, n2=0) { // os parâmetros podem ser pré-definidos, nesse caso, se não digitarmos um dos parâmetros, a função irá considerar esse parâmetro ==0
    return n1 + n2
}
console.log(soma(7))